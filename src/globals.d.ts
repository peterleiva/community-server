module "*.graphql" {
	import { DocumentNode } from "graphql";
	const schema: DocumentNode;

	export default schema;
}

module "*.gql" {
	import { DocumentNode } from "graphql";
	const schema: DocumentNode;

	export default schema;
}
