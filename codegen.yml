overwrite: true
schema:
  - src/loaders/apollo/*.graphql
  - src/modules/**/*.graphql
hooks:
  afterAllFileWrite:
    - npx prettier --write
    - npx eslint --fix
generates:
  src/types/schema.ts:
    config:
      mappers:
        User: modules/user/schema#UserDocument

    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
